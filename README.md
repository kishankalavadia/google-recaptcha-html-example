## Sample Google reCaptcha App

> Build using node.js express with ejs view

## How to run service?
- Install dependency `npm install`
- Run `nodemon ./bin/www` in bash.
- Open this url `http://localhost:3030`